<!DOCTYPE html>
<html>
<head>
    <title>Login - IT Lampang</title>
    <link href="https://fonts.googleapis.com/css2?family=Sarabun:wght@400;700&display=swap" rel="stylesheet">
    <style>
        body { font-family: 'Sarabun', sans-serif; display: flex; justify-content: center; align-items: center; height: 100vh; background: linear-gradient(135deg, #6c5ce7 0%, #a29bfe 100%); margin: 0; }
        .login-card { background: white; padding: 50px 40px; border-radius: 24px; box-shadow: 0 20px 50px rgba(0,0,0,0.2); width: 350px; text-align: center; }
        h2 { color: #2d3436; margin-bottom: 35px; font-size: 1.8rem; font-weight: 700; }
        input { width: 100%; padding: 15px; margin: 12px 0; border: 1px solid #dfe6e9; border-radius: 12px; box-sizing: border-box; font-size: 1rem; transition: 0.3s; background: #f9f9f9; }
        input:focus { border-color: #6c5ce7; outline: none; box-shadow: 0 0 10px rgba(108, 92, 231, 0.15); background: #fff; }
        button { width: 100%; padding: 16px; background: #6c5ce7; color: white; border: none; border-radius: 12px; cursor: pointer; font-size: 1.1rem; font-weight: bold; transition: 0.3s; margin-top: 15px; text-transform: uppercase; letter-spacing: 1px; }
        button:hover { background: #5649c0; transform: translateY(-2px); box-shadow: 0 8px 20px rgba(108, 92, 231, 0.3); }
        .logo-img { width: 90px; margin-bottom: 15px; filter: drop-shadow(0 4px 8px rgba(0,0,0,0.1)); }
    </style>
</head>
<body>
    <div class="login-card">
        <h2>เข้าสู่ระบบ</h2>
        <form action="/login" method="POST">
            <input type="text" name="username" placeholder="ชื่อผู้ใช้งาน" required>
            <input type="password" name="password" placeholder="รหัสผ่าน" required>
            <button type="submit">LOGIN</button>
        </form>
    </div>
</body>
</html>