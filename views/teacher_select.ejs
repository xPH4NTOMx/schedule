<!DOCTYPE html>
<html lang="th">
<head>
    <meta charset="UTF-8">
    <title>เลือกผู้สอน - IT Lampang</title>
    <style>
        body { font-family: 'Sarabun', sans-serif; background: #f4f7f6; margin: 0; padding: 40px; }
        .container { max-width: 1000px; margin: auto; text-align: center; }
        h1 { color: #2c3e50; margin-bottom: 30px; }
        .list-container { display: grid; grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 20px; }
        .card { 
            background: white; padding: 25px; border-radius: 15px; text-decoration: none; 
            color: #333; box-shadow: 0 4px 6px rgba(0,0,0,0.05); transition: 0.3s;
            border: 1px solid transparent; display: flex; flex-direction: column; align-items: center;
        }
        .card:hover { 
            border-color: #007bff; color: #007bff; transform: translateY(-5px); 
            box-shadow: 0 10px 20px rgba(0,123,255,0.1); 
        }
        .avatar { width: 60px; height: 60px; background: #e7f3ff; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-size: 24px; margin-bottom: 15px; }
        .card strong { font-size: 18px; }
        .btn-back { display: inline-block; margin-top: 40px; color: #666; text-decoration: none; font-weight: bold; }
        .btn-back:hover { color: #333; }
    </style>
</head>
<body>
    <div class="container">
        <h1>👨‍🏫 เลือกผู้สอนเพื่อดูตารางสอน</h1>
        <div class="list-container">
            <% teachers.forEach(t => { %>
                <a href="/teacher/schedule?teacherId=<%= t.id %>" class="card">
                    <div class="avatar">👨‍🏫</div>
                    <strong><%= t.fullname %></strong>
                    <small style="color: #888;">ID: <%= t.id %></small>
                </a>
            <% }) %>
        </div>
        <a href="/dashboard" class="btn-back">⬅️ กลับหน้าหลัก</a>
    </div>
</body>
</html>